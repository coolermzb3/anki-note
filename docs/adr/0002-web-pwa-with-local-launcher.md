# Web app with lightweight PWA support

The first version stays on the pnpm/Vite web stack and provides a Windows launcher script for convenient local startup. It also includes lightweight PWA support with a web app manifest and service worker for an app-like browser experience, while avoiding Electron or Tauri until there is a clear need for native desktop packaging.
